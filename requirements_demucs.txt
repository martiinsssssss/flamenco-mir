# Demucs Source Separation (install in separate environment)
# Note: Demucs and Spleeter have conflicting dependencies, use separate .venv

demucs>=4.0.0
torch>=2.0.0
torchaudio>=2.0.0
soundfile>=0.11.0
pandas
numpy
scipy
